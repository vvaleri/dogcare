extends ../base/layout

block variables
  - const title = "DogCare"
  - const hasHeader = true
  - const hasFooter = true


block main
  main
    section.main
      .main__bg
        +picture('content', 'bg-image', 'jpg', 1440, 808, 'woman with dalmatian')
      .main__content.container(data-aos='zoom-in')
        h1 Premier dog boarding, daycare &amp; day spa  
        +btn({btnType: "button", text: "Make an Appointment"})

